// Compatibility export for orchestration call sites excluded by #2829.
import * as RepositoryIdentityResolver from "../RepositoryIdentityResolver.ts";

export const RepositoryIdentityResolverLive = RepositoryIdentityResolver.layer;
