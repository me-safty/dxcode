# FORK-CHANGES.md — upstream divergence ledger

This fork (`atlasinc-global/vector-workspace`) rebases on `pingdotgg/t3code` forever
(RULE #1 in `AGENTS.md`). Every change to an **upstream-owned file** is recorded here so
rebases stay cheap. All Atlas customization that lives in NEW `atlas-*` files/dirs/packages
does **not** need an entry — this ledger is only for edits to files that also exist
upstream.

Each entry: **file · why · how to re-apply after rebase.**

| File | Why | How to re-apply after an upstream rebase |
| --- | --- | --- |
| `AGENTS.md` (and its `CLAUDE.md` symlink) | Replaced T3's agent guide with the Atlas Vector fork guide (Rule #1, two-backend architecture, FastAPI feature map). One intentional upstream-doc divergence. | Persistent divergence — on conflict keep the Atlas version; fold any genuinely useful new upstream guidance into the "Upstream T3 essentials" section at the bottom. |
| `apps/web/src/main.tsx` | (1) import the Atlas theme (`packages/atlas-theme/src/styles.css`) after `./index.css` so Atlas tokens win the cascade; (2) wrap the app element in `<AtlasAuthGate>` (FastAPI login gate, inert when `VITE_ATLAS_API_URL` is unset). | Re-add the theme import after `./index.css`, and re-wrap the app element in `<AtlasAuthGate>` (import from `./atlas/AtlasAuthGate`). |
| `apps/web/src/branding.ts` | `APP_BASE_NAME` resolves `injectedDesktopAppBranding?.baseName ?? VITE_ATLAS_APP_NAME ?? "Atlas Vector"` — the final fallback is now "Atlas Vector" (was "T3 Code") so the app name / `document.title` reads "Atlas Vector" even without the env var set (plain local dev). | Re-apply both: add `?? import.meta.env.VITE_ATLAS_APP_NAME` and change the literal default `"T3 Code"` → `"Atlas Vector"` in the `APP_BASE_NAME` chain. |
| `apps/web/src/branding.test.ts` | Follows the `branding.ts` default change: the "normalizes hosted app channel metadata" case (no injected branding, no env) now expects `APP_DISPLAY_NAME` = "Atlas Vector (Nightly)" instead of "T3 Code (Nightly)". (The "uses injected desktop branding" case still expects "T3 Code (Nightly)" — it stubs that value, unrelated to the default.) | Update only the no-injection assertion to "Atlas Vector (Nightly)"; leave the injected-branding test as-is. |
| `apps/web/src/routes/__root.tsx` | Mount `<AtlasDeals />` (deals picker → opens a deal's `/workspaces/<id>` as a T3 project) alongside the app shell, gated on `primaryEnvironmentAuthenticated`. | Re-add the `AtlasDeals` import and the `{primaryEnvironmentAuthenticated ? <AtlasDeals /> : null}` line after the app-shell block. |
| `apps/web/src/components/Sidebar.tsx` | Replaced the `T3Wordmark` "T3" mark (and its "Code" label) in the sidebar header with `AtlasWordmark` — a large `/atlas-logo.png` Atlas Vector logo (`h-12`, `dark:brightness-0 dark:invert` so it stays visible on dark themes). Asset is the new (non-upstream) file `apps/web/public/atlas-logo.png`. | Re-rename `T3Wordmark`→`AtlasWordmark`, swap the SVG body for the `<img src="/atlas-logo.png" className="h-12 …">`, and in `SidebarChromeHeader` replace `<T3Wordmark />` + the "Code" `<span>` with `<AtlasWordmark />` (keep the stage badge). |
| `apps/web/index.html` | Atlas branding for the pre-React boot shell + browser tab: (1) favicon links now point at the new square Atlas mark PNGs (`/atlas-favicon-32.png`, `/atlas-favicon-16.png`, `/atlas-apple-touch-icon.png`) instead of `/favicon.ico` + `/apple-touch-icon.png`; (2) the `#boot-shell-logo` splash image is `/atlas-logo.png` (sized `height:88px; width:auto`, card relaxed to `min-width:96px` + padding) with `html.dark` `brightness(0) invert(1)` so the dark-navy logo shows on the dark splash; (3) static `<title>` "T3 Code (Alpha)" → "Atlas Vector" (initial tab/hover title before JS sets the dynamic name); `alt`/`aria-label` → "Atlas Vector". | Re-point the favicon `<link>`s to the `atlas-*` icons, swap the boot-shell `<img src>` to `/atlas-logo.png`, re-apply the `#boot-shell-logo`/`#boot-shell-card` sizing + the `html.dark` filter rule, set `<title>` to "Atlas Vector", and rename the alt/aria text. New icon assets are non-upstream files under `apps/web/public/`. |
| `apps/web/src/components/SplashScreen.tsx` | React splash now shows the full `/atlas-logo.png` Atlas Vector logo (`h-22 w-auto`, `dark:brightness-0 dark:invert`) instead of the square `/apple-touch-icon.png` T3 mark; `alt`/`aria-label` → "Atlas Vector". | Swap the `<img src>` to `/atlas-logo.png`, replace the `size-16` classes with `h-22 w-auto object-contain dark:brightness-0 dark:invert`, and rename the alt/aria text. |
